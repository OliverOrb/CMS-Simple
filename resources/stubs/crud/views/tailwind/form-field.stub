<div class="mt-4">
    <x-forms.input
        label="{{title}}"
        name="{{column}}"
        :value="old('{{column}}', ${{modelNameLowerCase}}?->{{column}})"
        placeholder="{{title}}"
    />
</div>
